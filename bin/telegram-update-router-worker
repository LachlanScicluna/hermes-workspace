#!/usr/bin/env node
import { runTelegramUpdateRouterWorkerCli } from '../scripts/telegram-update-router-worker.mjs';

runTelegramUpdateRouterWorkerCli().catch((error) => {
  process.stderr.write(`${error.message}\n`);
  process.exitCode = 1;
});
