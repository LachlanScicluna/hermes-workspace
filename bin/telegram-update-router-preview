#!/usr/bin/env node
import { runTelegramUpdateRouterPreviewCli } from '../scripts/telegram-update-router-preview.mjs';

runTelegramUpdateRouterPreviewCli().catch((error) => {
  process.stderr.write(`${error.message}\n`);
  process.exitCode = 1;
});
