#!/usr/bin/env node
import { runTelegramGatewayQueueContractPreviewCli } from '../scripts/telegram-gateway-queue-contract.mjs';

runTelegramGatewayQueueContractPreviewCli().catch((error) => {
  process.stderr.write(`${error.message}\n`);
  process.exitCode = 1;
});
